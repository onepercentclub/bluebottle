name: Quality Checks
on:
  push:
    branches:
      - main
  pull_request: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  translations:
    name: Translations
    runs-on: self-hosted
    steps:
      - name: Fix workspace permissions (self-hosted)
        run: |
          if [ -n "${GITHUB_WORKSPACE}" ] && [ -d "${GITHUB_WORKSPACE}" ]; then
            sudo chown -R "$(whoami):$(id -gn)" "${GITHUB_WORKSPACE}" || true
          fi

      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Restore cached virtualenv
        uses: actions/cache/restore@v4
        with:
          key: env-${{ hashFiles('setup.py') }}-py39
          path: env

      - name: Create virtualenv (pyenv Python 3.9)
        run: |
          export PYENV_ROOT="${PYENV_ROOT:-$HOME/.pyenv}"
          export PATH="$PYENV_ROOT/bin:$PATH"
          PYTHON_BIN="$PYENV_ROOT/versions/3.9.21/bin/python"
          if [[ ! -x "$PYTHON_BIN" ]]; then
            echo "Python 3.9 not found at $PYTHON_BIN (expected pyenv with 3.9.21 on runner)" && exit 1
          fi
          "$PYTHON_BIN" -c "import sys; assert sys.version_info[:2] == (3, 9), f'Expected Python 3.9.x, got {sys.version}'"
          "$PYTHON_BIN" -m venv env

      - name: Show Python version
        run: |
          source env/bin/activate
          python --version

      - name: Install pip dependencies
        run: |
          source env/bin/activate
          python -m pip install "pip<24.1"
          pip install -e .[dev]

      - name: Saved cached virtualenv
        uses: actions/cache/save@v4
        with:
          key: env-${{ hashFiles('setup.py') }}-py39
          path: env

      - name: Extract messages
        run: |
          source env/bin/activate
          python manage.py makemessages --locale=en --ignore=env --settings=bluebottle.settings.testing

      - name: Check missing translations
        run: |
          source env/bin/activate
          pip install polib
          python ./locale/check_translations.py --locales=nl  > translation-message.txt

      - name: Echo
        run: cat translation-message.txt

      - name: Post comment
        uses: mshick/add-pr-comment@v2
        with:
          message-path: translation-message.txt
          refresh-message-position: true

  lint:
    name: Linting
    runs-on: self-hosted
    steps:
      - name: Fix workspace permissions (self-hosted)
        run: |
          if [ -n "${GITHUB_WORKSPACE}" ] && [ -d "${GITHUB_WORKSPACE}" ]; then
            sudo chown -R "$(whoami):$(id -gn)" "${GITHUB_WORKSPACE}" || true
          fi

      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Create virtualenv (pyenv Python 3.9)
        run: |
          export PYENV_ROOT="${PYENV_ROOT:-$HOME/.pyenv}"
          export PATH="$PYENV_ROOT/bin:$PATH"
          PYTHON_BIN="$PYENV_ROOT/versions/3.9.21/bin/python"
          if [[ ! -x "$PYTHON_BIN" ]]; then
            echo "Python 3.9 not found at $PYTHON_BIN (expected pyenv with 3.9.21 on runner)" && exit 1
          fi
          "$PYTHON_BIN" -c "import sys; assert sys.version_info[:2] == (3, 9), f'Expected Python 3.9.x, got {sys.version}'"
          "$PYTHON_BIN" -m venv env

      - name: Show Python version
        run: |
          source env/bin/activate
          python --version

      - name: Install pip dependencies
        run: |
          source env/bin/activate
          python -m pip install "pip<24.1"
          pip install -e .[dev]

      - name: Lint
        run: |
          source env/bin/activate
          flake8 .
