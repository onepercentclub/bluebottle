name: Quality Checks
on:
  push:
    branches:
      - main
  pull_request: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  translations:
    name: Translations (Debian ${{ matrix.debian }})
    runs-on: self-hosted
    strategy:
      fail-fast: false
    container: ${{ matrix.container_image }}
    steps:
      - name: Install Python 3.9 (Debian 13 only)
        run: |
          apt-get update
          apt-get install -y --no-install-recommends python3.9 python3.9-venv python3.9-dev
          update-alternatives --install /usr/bin/python3 python3 /usr/bin/python3.9 1
          update-alternatives --install /usr/bin/python python /usr/bin/python3.9 1

      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Restore cached virtualenv
        uses: actions/cache/restore@v4
        with:
          key: env-${{ hashFiles('setup.py') }}-debian-${{ matrix.debian }}
          path: env

      - name: Install venv support (Debian slim)
        if: matrix.debian == '11'
        run: |
          apt-get update
          apt-get install -y --no-install-recommends python3-venv

      - name: Create venv
        run: |
          python3.9 -m venv env 2>/dev/null || python -m venv env

      - name: Show Python version
        run: |
          source env/bin/activate
          python --version

      - name: Install pip dependencies
        run: |
          source env/bin/activate
          python -m pip install "pip<24.1"
          pip install -e .[dev]

      - name: Saved cached virtualenv
        uses: actions/cache/save@v4
        with:
          key: env-${{ hashFiles('setup.py') }}-debian-${{ matrix.debian }}
          path: env

      - name: Extract messages
        run: |
          source env/bin/activate
          python manage.py makemessages --locale=en --ignore=env --settings=bluebottle.settings.testing

      - name: Check missing translations
        run: |
          source env/bin/activate
          pip install polib
          python ./locale/check_translations.py --locales=nl  > translation-message.txt

      - name: Echo
        run: cat translation-message.txt

      - name: Post comment
        uses: mshick/add-pr-comment@v2
        if: always()
        with:
          message-path: translation-message.txt
          refresh-message-position: true

  lint:
    name: Lint (Debian ${{ matrix.debian }})
    runs-on: self-hosted
    strategy:
      fail-fast: false
    container: ${{ matrix.container_image }}
    steps:
      - name: Install Python 3.9 (Debian 13 only)
        run: |
          apt-get update
          apt-get install -y --no-install-recommends python3.9 python3.9-venv python3.9-dev
          update-alternatives --install /usr/bin/python3 python3 /usr/bin/python3.9 1
          update-alternatives --install /usr/bin/python python /usr/bin/python3.9 1

      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Install venv support (Debian slim)
        if: matrix.debian == '11'
        run: |
          apt-get update
          apt-get install -y --no-install-recommends python3-venv

      - name: Create venv
        run: |
          python3.9 -m venv env 2>/dev/null || python -m venv env

      - name: Show Python version
        run: |
          . env/bin/activate
          python --version

      - name: Install pip dependencies
        run: |
          . env/bin/activate
          python -m pip install "pip<24.1"
          pip install -e .[dev]

      - name: Lint
        run: |
          . env/bin/activate
          flake8 .
