# -*- coding: utf-8 -*-
# Generated by Django 1.11.17 on 2021-02-18 11:45
from __future__ import unicode_literals

from django.db import migrations, connection

from bluebottle.clients import properties
from bluebottle.clients.models import Client
from bluebottle.clients.utils import LocalTenant
from bluebottle.utils.utils import update_group_permissions


def add_group_permissions(apps, schema_editor):
    tenant = Client.objects.get(schema_name=connection.tenant.schema_name)
    with LocalTenant(tenant):
        group_perms = {
            'Staff': {
                'perms': (
                    'add_deed', 'change_deed', 'delete_deed',
                )
            },
            'Anonymous': {
                'perms': (
                    'api_read_deed',
                ) if not properties.CLOSED_SITE else ()
            },
            'Authenticated': {
                'perms': (
                    'api_read_deed', 'api_add_own_deed', 'api_change_own_deed', 'api_delete_own_deed',
                )
            }
    }

    update_group_permissions('deeds', group_perms, apps)


class Migration(migrations.Migration):

    dependencies = [
        ('deeds', '0002_auto_20210218_1248'),
    ]

    operations = [
        migrations.RunPython(
            add_group_permissions,
            migrations.RunPython.noop
        )
    ]
