# Generated by Django 4.2.20 on 2025-08-20 07:05
from bluebottle.utils.utils import update_group_permissions

from bluebottle.clients.utils import LocalTenant

from bluebottle.clients.models import Client
from django.contrib.auth.models import Group, Permission
from django.contrib.contenttypes.models import ContentType
from django.db import migrations
from django.db import connection



def create_staff_permissions(apps, schema_editor):
    def add_group_permissions(apps, schema_editor):
        tenant = Client.objects.get(schema_name=connection.tenant.schema_name)
        with LocalTenant(tenant):
            group_perms = {
                'Staff': {
                    'perms': (
                        'add_grantapplication', 'change_grantapplication', 'delete_grantapplication',
                        'add_grantdonor', 'change_grantdonor', 'delete_grantdonor',
                        'add_grantpayout', 'change_grantpayout', 'delete_grantpayout',
                    )
                },
            }

        update_group_permissions('grant_management', group_perms, apps)


class Migration(migrations.Migration):

    dependencies = [
        (
            "grant_management",
            "0008_alter_grantpayout_provider_alter_grantprovider_owner",
        ),
    ]

    operations = [
        migrations.RunPython(create_staff_permissions, migrations.RunPython.noop),
    ]
