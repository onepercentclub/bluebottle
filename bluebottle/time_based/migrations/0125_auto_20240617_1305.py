# Generated by Django 3.2.20 on 2024-06-17 11:05
from django.db import migrations, connection

from bluebottle.utils.utils import update_group_permissions

from bluebottle.clients import properties
from bluebottle.clients.models import Client
from bluebottle.clients.utils import LocalTenant


def add_group_permissions(apps, schema_editor):
    tenant = Client.objects.get(schema_name=connection.tenant.schema_name)
    with LocalTenant(tenant):
        group_perms = {
            "Staff": {
                "perms": (
                    "add_team",
                    "change_team",
                    "delete_team",
                    "add_teammember",
                    "change_teammember",
                    "delete_teammember",
                )
            },
            "Anonymous": {
                "perms": (
                    (
                        "api_read_team",
                        "api_read_teammember",
                    )
                    if not properties.CLOSED_SITE
                    else ()
                )
            },
            "Authenticated": {
                "perms": (
                    "api_read_team",
                    "api_add_own_team",
                    "api_change_own_team",
                    "api_delete_own_team",
                    "api_read_teammember",
                    "api_add_own_teammember",
                    "api_change_own_teammember",
                    "api_delete_own_teammember",
                )
            },
        }

    update_group_permissions("time_based", group_perms, apps)


class Migration(migrations.Migration):

    dependencies = [
        ("time_based", "0124_auto_20240617_1303"),
    ]

    operations = [
        migrations.RunPython(add_group_permissions, migrations.RunPython.noop)
    ]
