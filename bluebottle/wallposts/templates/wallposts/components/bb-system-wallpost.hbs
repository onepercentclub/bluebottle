{% load templatetag_handlebars %}
{% load i18n %}
{% load static %}

{% tplhandlebars "components/bb-system-wallpost" %}

	<article class="m-wallpost m-wallpost-system">

	    <header class="wallpost-header">
	        <figure class="user-avatar">
                {{#if author}}
    	        	<img {{bindAttr src="post.author.getAvatar" alt="post.author.full_name"}} />
                {{else}}
                    <img src="{% get_static_prefix %}/images/default-avatar.png" alt="someone" />
                {{/if}}

	        </figure>

            {{#if post.author}}
	            <a {{action openInBigBox "userModal" post.author}} class="user-name">
                    <strong>{{post.author.full_name}}</strong>
                </a> {% trans "donated" %} <strong>&euro; {{localize donation.amount}}</strong>
            {{else}}
            <strong>{% trans "Guest" %}</strong> {% trans "donated" %} <strong>&euro; {{localize donation.amount}}</strong>
            {{/if}}

	        <abbr class="timestamp">{{ localize post.created formatting="X" }}</abbr>
	    </header>

	    <ul class="comments">
			{{bb-wallpost-comment-list comments=post.reactions post=post currentUser=currentUser}}
	    </ul>

	</article>

{% endtplhandlebars %}
