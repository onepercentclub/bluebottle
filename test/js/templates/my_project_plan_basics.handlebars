<legend>
        <strong>Project basics</strong>
    </legend>

    <fieldset>
        <ul>
            <li class="control-group has-popover" data-content="Be short, creative, simple and memorable.">
                <label class="control-label">Title</label>
                <div class="controls">
                    {{view Em.TextField valueBinding="title" placeholder="Title" classBinding="errors.title.length:error"}}
                </div>

                {{#if errors.title}}
                    <div class="has-errors">{{#each errors.title}}<p>{{this}}</p>{{/each}}</div>
                {{/if}}
            </li>

            <li class="control-group has-popover" data-content="Pitch your smart idea in one sentence.">
                <label class="control-label">Pitch</label>
                <div class="controls">
                    {{view Em.TextArea valueBinding="pitch" placeholder="Description" classBinding="errors.pitch.length:error"}}
                </div>

                {{#if errors.pitch}}
                    <div class="has-errors">{{#each errors.pitch}}<p>{{this}}</p>{{/each}}</div>
                {{/if}}
            </li>
        </ul>
    </fieldset>
    <fieldset>
        <ul>
            <li class="control-group has-popover" data-content="Select one of the themes.">
                <label class="control-label">Theme</label>
                <div class="controls">
                    {{view App.ThemeSelectView valueBinding="theme" classBinding="errors.theme.length:error"}}
                </div>

                {{#if errors.theme}}
                    <div class="has-errors">{{#each errors.theme}}<p>{{this}}</p>{{/each}}</div>
                {{/if}}
            </li>
        </ul>
    </fieldset>
    
    <fieldset>
        <ul>
            <li class="control-group">
                <label class="control-label">
                    Tags ({{tags.length}})<br>
                    <small>Between 2 and 5 tags</small>
                </label>
                <div class="controls">
                    {{view App.TagWidget tagsBinding="tags" classBinding="errors.tags.length:error"}}
                </div>

                {{#if errors.tags}}
                    <div class="has-errors">{{#each errors.tags}}<p>{{this}}</p>{{/each}}</div>
                {{/if}}
            </li>
        </ul>
    </fieldset>
    <fieldset>
        <ul>
            <li class="control-group has-popover" data-content="What do you need to realise your smart idea?">
                <label class="control-label">
                    What do you need?
                </label>

                <div class="controls">
                    {{#view Em.RadioButtonGroup name="need" selectedValueBinding="need" class="radio-group"}}
                        <label class="radio">{{view view.RadioButton value="skills"}}<span>Skills and expertise</span></label>
                        <label class="radio">{{view view.RadioButton value="finance"}}<span>Crowdfunding campaign</span></label>
                        <label class="radio">{{view view.RadioButton value="both"}}<span>Both</span></label>
                    {{/view}}
                </div>
            </li>

        </ul>
    </fieldset>

    {{#if isDirty}}
        <button><span class="flaticon solid right-2"></span>Save & Next</button>
    {{else}}
        <button><span class="flaticon solid right-2"></span>Next</button>
    {{/if}}


    {{partial "my_project_tasks_and_updates"}}